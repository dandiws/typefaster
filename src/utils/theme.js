export function createColorTheme({
  background,
  text,
  typingBackground,
  untypedLetter,
  correctLetter,
  incorrectLetter,
  extraLetter,
  caret,
}) {
  return {
    background,
    text,
    typingBackground,
    untypedLetter,
    correctLetter,
    incorrectLetter,
    extraLetter,
    caret,
  }
}
