export function createColorTheme({
  background,
  text,
  typingBackground,
  untypedLetter,
  correctLetter,
  incorrectLetter,
  extraLetter,
  caret,
}: any) {
  return {
    background,
    text,
    typingBackground,
    untypedLetter,
    correctLetter,
    incorrectLetter,
    extraLetter,
    caret,
  }
}
